version: '3'
services:
  hbase:
    image: harisekhon/hbase:1.3
    container_name: hbase
    ports:
      - "16010:16010"
    # volumes:
    #   - "${HBASE_DIR}:/hbase"
    networks:
      - net-hbase

networks:
  net-hbase:
    external: false
